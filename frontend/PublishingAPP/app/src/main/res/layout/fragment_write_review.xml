<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="?colorSurfaceVariant"
    android:layout_marginBottom="60dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Заголовок -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Написать рецензию"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/purple_80"
            android:gravity="center"
            android:layout_marginBottom="24dp"/>

        <!-- Комментарий -->
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Комментарий (опционально)"
            style="@style/Widget.Material3.TextInputLayout.OutlinedBox"
            android:layout_marginBottom="24dp">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etComment"
                android:layout_width="match_parent"
                android:layout_height="150dp"
                android:gravity="start"
                android:inputType="textMultiLine"
                android:maxLines="10"
                android:minLines="5"/>

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Решение рецензента -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ваше решение по заказу:"
            android:textSize="16sp"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"/>

        <RadioGroup
            android:id="@+id/radioGroupDecision"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginBottom="24dp">

            <RadioButton
                android:id="@+id/rbApprove"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Одобрить (статус: Готов к печати)"
                android:textSize="14sp"
                android:layout_marginBottom="12dp"
                android:padding="12dp"
                android:background="?selectableItemBackground"/>

            <RadioButton
                android:id="@+id/rbReject"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Отклонить (статус: Отменён)"
                android:textSize="14sp"
                android:layout_marginBottom="12dp"
                android:padding="12dp"
                android:background="?selectableItemBackground"/>

            <RadioButton
                android:id="@+id/rbRevision"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Отправить на доработку (статус: Редактируется)"
                android:textSize="14sp"
                android:padding="12dp"
                android:background="?selectableItemBackground"/>

        </RadioGroup>

        <!-- Подсказка -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            app:cardBackgroundColor="#E3F2FD"
            app:strokeColor="#2196F3"
            app:strokeWidth="1dp"
            app:cardCornerRadius="8dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Внимание!"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="#1565C0"
                    android:layout_marginBottom="8dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="После отправки решения статус заказа изменится автоматически. Действие нельзя будет отменить."
                    android:textSize="13sp"
                    android:textColor="#0D47A1"
                    android:lineSpacingExtra="4dp"/>

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Кнопка отправки -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSubmit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Отправить решение"
            android:textSize="16sp"
            style="@style/Widget.Material3.Button"
            app:cornerRadius="8dp"
            android:layout_marginBottom="16dp"/>

        <!-- Прогресс -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:visibility="gone"/>

    </LinearLayout>

</androidx.core.widget.NestedScrollView>